Day 1 (unreleased): 
        - Started Developing the Dungeon Crawler Game
        - Made functions (unfinished):

        . void choosePlayerName
        . void lobbyArea
        . void checkStats
        . void checkInventory
        . void shopArea
        . void caveArea

        - Started developing a "Already Purchased Checker" which will later be in voidedVerification function.

Day 2 (v0.1):
        - Started developing caveArea
        - Added voidedVerification function, that checks whether an item was already purchased.
        
        - Changed choosePlayerName function, which now lets you choose character's name instead of picking one of 5 premade names

        - Fixed many logic issues
        - Fixed menu navigation problem by removing function calls at the end of each function, that caused unexpected code behaviour. Replaced by making each function menu in a while loop, which is stable.
      (v0.11):
        - Added build-Debug, build-Release, .exe binary
Day 3 (v0.12):
        - Almost finished inventory system (Item interaction, weapon equipment)
        - Made multifile system, each cpp file for different kind of functions
Day 4 (v0.13):
        - Added inventoryInteraction function that is the core of inventory operability and interaction with items
        - Added equipped/unequipped suffixes in invetory for items that are affected by isEquipped bool
        - Fixed health potion bug that broke itemlist when consumed (fixed by adding an eraseIndex that adds to itemlist.erase(itemlist.begin()) a corresponding index depending on which itemlist slot is the potion currently located
        - Changed vector<bool> isEquipped size from 3 to undefined (Now the size of isEquipped will be expanded by 1, each time a corresponding item is bought
        - Added an iterator that checks for true values inside isEquipped, thank to that I implemented a system that allows to equip only one weapon at a time
Day 5 (v0.14)
        - A massive rework of inventory system was done. I noticed that having multiple unrelated vectors and variables becomes more and more unstable and unreliable each time I expanded inventory system's functionnality
	      that's why I added a struct that assembles variables and vectors related to items from the shop.
        - Now potions will be stacked instead of being a separate element of inventory for each potion, current limit of having potions at once is 5
        - Started developing first cave location
Day 7 (v0.14.1)
        - Fixed logic issues with loot chest drop chance conditions
        - Replaced rand random number generation by a better one (mt19937) which has a much higher randomization quality + updates
        - Now chests in ClaystoneCave can't be found if player steps back
Day 8 (v0.15)
        - Now you can encounter a Cave Rat enemy in Claystone Cave area 
        - Now if player health reaches 0, a death message will appear and player will be sent back to Main Menu
        - Now choosePlayerName will only be prompted when the player enters Main Area the first time
        - Fixed nested loop cin issues by separating input char option variables for different loop layers
Day 9 (v0.16)
        - Now if player has full hp, he won't be able to use a health potion, also if health goes beyond 100 it'll be set to 100
        - Now items are only allowed to have one isEquipped in state True (carry only one weapon at a time)
        - Now when an enemy is killed, the next time it'll be encountered, its health will be restored
        - Added Overgrown Spider to Claystone Caves!
        - Fixed enemy encounter probability bugs
Day 10 (v0.17)
        - Replaced repetetive code with functions
       (v0.20) (BIG UPDATE)
        - Added enum itemTypes with CONSUMABLE and WEAPON to game.h
        - Removed isConsumable bool from Items struct, replaced with itemTypes type
        - Added bonusdamage field to Items struct
        - Updated all item definitions in main.cpp to use .type and .bonusdamage instead of isConsumable
        - Refactored inventoryInteraction to branch on inventorySlot.type instead of comparing names against hardcoded shop positions
        - Updated itemEquipment to use inventorySlot.name and inventorySlot.bonusdamage instead of hardcoded values
        - Replaced the hardcoded 8-case switch in checkInventory with a dynamic loop using stoi, bounds checking, and a zero guard
       (v0.20.1 and v0.20.2)
        - Changed default health and balance values to "playable" values
        - Removed unused cpp file that was initially a prototype of inventory.cpp (inventorySystem.cpp)
        - Moved legacy binaries inside respective folder
        - !! these version can vary from description in changelog.txt and github repo, since I made some mistakes and had to repush commits !!
Day 11 (v0.21)
        - Added Level system (structs were expanded)
        - Changed the way stats are shown in stats and areas
        - Changed version to current in Main Menu (very hard work)
        - Now maxHealth will increase depending on player's level (+5 to maxHealth each new lvl)
        - added coin rewards at level-ups
Day 12 (v0.21.1)
        - Fixed enemyFight lingering until enemy is dead even if the player died midfight (now as soon as player.health reaches 0, the fight will be lost thus the loop will be exited
        - Since enemyFight was changed, bool exitLoop became redundant and was removed
        - Overall restructure of enemyFight function
        - Forgot to build exe binary :)
Day 13 (v0.22)
        - Added a new consumable item: Telecrystl
        - Refactored inventory system since the previous version was made for only one consumable
        - Fixed inventoryCheck relying on the shopitems.at(i) order (which is not relevant anymore, since we've got multiple consumables and the first that is bought is not always the first in the shopitems list)
        - wrapped codeblock responsible for consumable items purchasing inside function buyConsumable
          (used bool function approach to compensate the inability to use loop control statements inside buyConsumable, since function's body obviously have no idea about the loop outside the function
        


        
